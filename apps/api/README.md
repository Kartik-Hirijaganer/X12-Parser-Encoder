# eligibility-workbench-api

FastAPI scaffold for the X12 eligibility workbench.

Phase 0 includes only the application shell, PHI-safe request-correlation middleware, and
smoke-test coverage so later phases can add endpoint contracts without reworking the runtime.
